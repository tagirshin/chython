version: 2

build:
  os: "ubuntu-22.04"
  tools:
    python: "3.10"
  jobs:
    post_create_environment:
      - pip install uv
    post_install:
      - uv pip install --group docs .

sphinx:
  builder: html
  configuration: doc/conf.py
